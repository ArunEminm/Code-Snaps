#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#define print(x) printf("%d\n",x)
#define take(x) scanf("%d",&x)

int main()
{
	int t,m,n,*x,*y;
	take(t);
	while(t-->0)
	{
		take(m);take(n);
		x=(int *)malloc(n*sizeof(int));
		y=(int *)malloc(m*sizeof(int));
		
	}
}